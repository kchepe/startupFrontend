import { Grid } from "@mui/material";
import React from "react";

const Tracker = () => {
  return (
    <div>
      <Grid container spacing={1}>
        <Grid item xs={12}>
          <h1>Tracker</h1>
        </Grid>
      </Grid>
    </div>
  );
};

export default Tracker;
